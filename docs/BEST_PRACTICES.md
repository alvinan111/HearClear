# 🎓 HearClear 代码最佳实践\n\n## 目录\n\n1. [错误处理](#错误处理)\n2. [API 调用](#api-调用)\n3. [状态管理](#状态管理)\n4. [输入验证](#输入验证)\n5. [性能优化](#性能优化)\n6. [测试](#测试)\n7. [日志和调试](#日志和调试)\n8. [常见陷阱](#常见陷阱)\n\n---\n\n## 错误处理\n\n### ✅ 正确的做法\n\n```typescript\n// 1. 总是捕获并记录错误\ntry {\n  await riskyOperation();\n} catch (error) {\n  logger.error('Module', 'Operation failed', error as Error);\n  throw error; // 如果需要传播错误\n}\n\n// 2. 提供有意义的错误信息\nif (error instanceof NetworkError) {\n  errorReporter.captureException(error, { type: 'network' });\n  showToast('网络连接失败，请重试');\n} else if (error instanceof ValidationError) {\n  logger.warn('Validation', error.message);\n  showToast('输入数据有误');\n}\n\n// 3. 使用 apiCall 包装器自动处理\nconst result = await apiCall('Service', async () => {\n  // 你的异步操作\n  return data;\n}, { maxRetries: 3 });\n\nif (result.error) {\n  // 错误已被记录和上报\n  handle(result.error);\n}\n```\n\n### ❌ 避免的做法\n\n```typescript\n// ❌ 忽略错误\nawait operation(); // 如果失败怎么办？\n\n// ❌ 模糊的错误消息\ncatch (error) {\n  console.log('Something went wrong'); // 不知道什么出错了\n}\n\n// ❌ 直接显示错误对象给用户\ncatch (error) {\n  Alert.alert(error); // 技术细节暴露给用户\n}\n```\n\n---\n\n## API 调用\n\n### ✅ 正确的做法\n\n```typescript\n// 1. 使用 apiCall 包装（自动重试、超时、日志）\nimport { apiCall, supabaseCall } from '@services/api-client';\n\nconst { data, error } = await apiCall(\n  'UserService',\n  async () => {\n    const { data, error } = await supabase\n      .from('users')\n      .select()\n      .eq('id', userId)\n      .single();\n\n    if (error) throw error;\n    return data;\n  },\n  { maxRetries: 3, timeout: 5000 }\n);\n\n// 2. 处理错误结果\nif (error) {\n  if (isRetryable(error)) {\n    // 此时 apiCall 已经重试过了\n  }\n  return null;\n}\n\nreturn data;\n\n// 3. 记录慢请求\nif (duration > 3000) {\n  errorReporter.captureMessage(\n    `Slow API: ${duration}ms`,\n    'warning'\n  );\n}\n```\n\n### ❌ 避免的做法\n\n```typescript\n// ❌ 直接调用，不处理错误\nconst { data } = await supabase.from('users').select();\nreturn data; // 可能是 null\n\n// ❌ 重复编写错误处理\nconst { data, error } = await supabase.from('a').select();\nif (error) { /* 处理 */ }\n\nconst { data: data2, error: error2 } = await supabase.from('b').select();\nif (error2) { /* 处理 */ }\n```\n\n---\n\n## 状态管理\n\n### ✅ 正确的做法\n\n```typescript\n// 1. 使用细粒度 Selector（避免不必要重渲染）\nimport { useAudioStore, audioStoreSelectors } from '@stores/audio-store-optimized';\n\nfunction AudioControls() {\n  // ✓ 只订阅需要的字段\n  const isRunning = useAudioStore(s => s.status === 'running');\n  const gain = useAudioStore(s => s.params.gain);\n  const audiogramAndPrescription = useAudioStore(\n    audioStoreSelectors.selectAudiogramAndPrescription\n  );\n\n  return (\n    <View>\n      <Button title={isRunning ? '停止' : '开始'} />\n      <Slider value={gain} />\n    </View>\n  );\n}\n\n// 2. 分离 Store（职责分离）\n// ✓ 分离关注点\nconst useAuthStore = create(...); // 只管理认证\nconst useAudioStore = create(...); // 只管理音频\nconst useUIStore = create(...);    // 只管理 UI\n\n// 3. 避免 Store 中的派生数据\n// ✓ Store 中只放原始状态\nconst useAudioStore = create(set => ({\n  params: initialParams,\n  status: 'idle',\n  setStatus: (s) => set({ status: s }),\n  // ✓ 派生数据在组件中计算\n}));\n\nfunction AudioStatus() {\n  const status = useAudioStore(s => s.status);\n  // ✓ 在组件中派生\n  const isReady = status === 'ready';\n  const isRunning = status === 'running';\n  return <Text>{isRunning ? '运行中' : '空闲'}</Text>;\n}\n```\n\n### ❌ 避免的做法\n\n```typescript\n// ❌ 订阅整个 Store\nconst store = useAudioStore(); // 任何字段改变都重渲染\n\n// ❌ Store 中混合太多功能\nconst useAppStore = create(...); // 包含 auth + audio + ui + settings\n\n// ❌ Store 中包含派生数据\nconst useAudioStore = create(set => ({\n  params: {},\n  // ❌ 派生数据在 Store 中计算\n  isRunning: computed(() => status === 'running'),\n}));\n```\n\n---\n\n## 输入验证\n\n### ✅ 正确的做法\n\n```typescript\nimport { validators, safeCast, validateMultiple } from '@utils/validators';\n\n// 1. 验证用户输入\nfunction LoginForm() {\n  const [phone, setPhone] = useState('');\n\n  const handleLogin = () => {\n    // ✓ 验证电话\n    const phoneValidation = validators.chinesePhone(phone);\n    if (!phoneValidation.valid) {\n      showError(phoneValidation.error);\n      return;\n    }\n\n    // ✓ 清理输入\n    const cleanPhone = validators.sanitize(phone);\n    performLogin(cleanPhone);\n  };\n}\n\n// 2. 多字段验证\nconst result = validateMultiple({\n  username: {\n    value: input.username,\n    validator: validators.nonEmptyString(input.username, 'Username'),\n  },\n  email: {\n    value: input.email,\n    validator: validators.email(input.email),\n  },\n  age: {\n    value: input.age,\n    validator: validators.numberInRange(input.age, 18, 120, 'Age'),\n  },\n});\n\nif (!result.valid) {\n  result.errors.forEach(err => logger.warn('Form', err));\n  return;\n}\n\n// 3. 安全类型转换\nconst settings = {\n  volume: safeCast.toNumber(userInput.volume, 50), // 失败返回 50\n  isDarkMode: safeCast.toBoolean(userInput.isDarkMode, false),\n  language: safeCast.toString(userInput.language, 'en'),\n};\n```\n\n### ❌ 避免的做法\n\n```typescript\n// ❌ 相信用户输入\nconst phone = phoneInput; // 可能不是有效电话号\nconst age = parseInt(ageInput); // 可能返回 NaN\nconst isDarkMode = ageInput === 'true'; // 类型不安全\n\n// ❌ 显示技术错误给用户\nif (phoneValidation.error) {\n  showError(phoneValidation.error); // 可能是内部错误信息\n}\n```\n\n---\n\n## 性能优化\n\n### ✅ 正确的做法\n\n```typescript\n// 1. 测量性能\nimport { PerformanceMark, performanceMonitor } from '@services/performance-monitor';\n\nconst mark = new PerformanceMark('audio-processing');\nconst result = await processAudio(data);\nmark.end(); // 自动记录\n\n// 2. 记录网络请求\nawait apiCall('API', async () => {\n  const start = Date.now();\n  const response = await fetch(url);\n  performanceMonitor.recordNetworkRequest(url, Date.now() - start);\n  return response;\n});\n\n// 3. 监控音频延迟\nperformanceMonitor.recordAudioMetrics({\n  outputLatency: 45, // ms\n  processingTime: 12,\n  droppedFrames: 0,\n});\n\n// 4. 在组件中使用 useMemo 和 useCallback\nfunction AudioControls() {\n  const gain = useAudioStore(s => s.params.gain);\n\n  // ✓ 缓存计算结果\n  const gainLabel = useMemo(() => {\n    return `Gain: ${gain.toFixed(1)}dB`;\n  }, [gain]);\n\n  // ✓ 缓存回调\n  const handleGainChange = useCallback((value: number) => {\n    useAudioStore.setState(s => ({\n      params: { ...s.params, gain: value },\n    }));\n  }, []);\n\n  return <Controls label={gainLabel} onChange={handleGainChange} />;\n}\n```\n\n### ❌ 避免的做法\n\n```typescript\n// ❌ 不监控性能\nawait operation(); // 花了多长时间？\n\n// ❌ 频繁的对象创建\nfunction List() {\n  return items.map(item => {\n    // ❌ 每次都创建新回调\n    return (\n      <Item\n        key={item.id}\n        onPress={() => handlePress(item)} // 新的函数实例\n      />\n    );\n  });\n}\n\n// ❌ Store 的 Selector 每次都返回新对象\nconst audioAndPrescription = useAudioStore(() => ({\n  audiogram: get().params.audiogram,\n  prescription: get().params.prescription,\n})); // 每次都是新对象？\n```\n\n---\n\n## 测试\n\n### ✅ 正确的做法\n\n```typescript\nimport { testUtils, testData } from '@tests/test-utils';\n\n// 1. 为关键功能编写测试\ndescribe('AuthService', () => {\n  it('should sign in with valid credentials', async () => {\n    const testUser = testData.createTestUser();\n    const result = await authService.login(testUser.phone);\n    expect(result.user).not.toBeNull();\n  });\n\n  it('should reject invalid phone numbers', () => {\n    const result = validators.chinesePhone('invalid');\n    expect(result.valid).toBe(false);\n  });\n});\n\n// 2. 使用测试数据生成器\nconst phone = testData.randomPhone();\nconst email = testData.randomEmail();\nconst user = testData.createTestUser({ nickname: 'Test' });\n\n// 3. 测量性能\nconst { duration } = await testUtils.measureTime(async () => {\n  return await expensiveOperation();\n});\nexpect(duration).toBeLessThan(1000);\n```\n\n### ❌ 避免的做法\n\n```typescript\n// ❌ 没有测试\n// ... 代码没有单元测试\n\n// ❌ 使用硬编码值\nit('should work', () => {\n  const user = { id: '123', name: 'test' }; // 硬编码\n});\n\n// ❌ 依赖外部服务\nit('should call API', async () => {\n  const result = await fetch('https://api...'); // 依赖实际 API\n});\n```\n\n---\n\n## 日志和调试\n\n### ✅ 正确的做法\n\n```typescript\nimport { logger } from '@services/logger';\nimport { errorReporter } from '@services/error-reporter';\n\n// 1. 使用结构化日志\nlogger.info('Module', 'Operation started', {\n  userId: user.id,\n  action: 'login',\n  timestamp: Date.now(),\n});\n\n// 2. 不同级别的日志\nlogger.debug('Module', 'Debug info', { detail });\nlogger.info('Module', 'Event occurred', { data });\nlogger.warn('Module', 'Warning message', { warning });\nlogger.error('Module', 'Error message', error, { context });\n\n// 3. 记录性能指标\nlogger.perf('Module', 'API call duration', 234);\n\n// 4. 上报关键错误\nerrorReporter.captureException(error, { severity: 'critical' });\nerrorReporter.addBreadcrumb('user-action', 'Button clicked');\n```\n\n### ❌ 避免的做法\n\n```typescript\n// ❌ 使用 console.log\nconsole.log('user:', user); // 模糊，生产环境看不到\n\n// ❌ 记录敏感信息\nlogger.info('API', 'Response', { password: user.password }); // 泄露密钥\n\n// ❌ 在生产环境输出调试信息\nif (__DEV__) {\n  console.log(debugInfo); // 但在发布版本中会打印？\n}\n```\n\n---\n\n## 常见陷阱\n\n### 1. Token 过期未处理\n\n```typescript\n// ❌ 错误\nawait apiCall('API', async () => {\n  return fetch(url, { headers: { auth: token } });\n});\n\n// ✅ 正确\nconst validToken = await tokenManager.getValidAccessToken();\nif (!validToken) {\n  await authService.logout();\n  return;\n}\nawait apiCall('API', async () => {\n  return fetch(url, { headers: { auth: validToken } });\n});\n```\n\n### 2. AsyncStorage 值类型错误\n\n```typescript\n// ❌ 错误\nconst count = await AsyncStorage.getItem('count');\nconst total = count + 10; // '10' + 10 = '1010'\n\n// ✅ 正确\nconst count = await encryptedAsyncStorage.get<number>('count');\nconst total = safeCast.toNumber(count, 0) + 10;\n```\n\n### 3. 内存泄漏\n\n```typescript\n// ❌ 错误\nuseEffect(() => {\n  const subscription = store.subscribe(handleChange);\n  // 忘记清理\n});\n\n// ✅ 正确\nuseEffect(() => {\n  const subscription = store.subscribe(handleChange);\n  return () => subscription.unsubscribe();\n}, []);\n```\n\n### 4. 闭包问题\n\n```typescript\n// ❌ 错误\nconst selector = () => {\n  const value = store.getState().value;\n  return { value }; // 每次都是新对象\n};\n\n// ✅ 正确\nconst selector = (state) => state.value; // 直接返回原始值\n```\n\n---\n\n## 总结\n\n| 原则 | 说明 |\n|---|---|\n| **显式错误处理** | 总是捕获和记录错误，不要默默失败 |\n| **使用工具函数** | 利用 `apiCall`、`validators` 等保证一致性 |\n| **细粒度状态** | Store 拆分、使用 Selector，避免不必要重渲染 |\n| **输入验证** | 不信任用户输入，总是验证和清理 |\n| **性能监控** | 测量关键操作，监控慢请求 |\n| **充分测试** | 关键路径必须有测试 |\n| **结构化日志** | 使用标准化日志，便于分析 |\n\n遵循这些最佳实践，你的代码将更健壮、更易维护、更高效！\n"