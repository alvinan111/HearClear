---
description: 每次运行测试后必须给用户一份结构化测试报告
alwaysApply: true
---

# 测试报告约定

在本项目中**每次执行测试**（如 `npm run test`、`test:coverage`、`test:db`、`test:audio-regression`）后，必须向用户提供一份**结构化测试报告**，不得只贴原始终端输出。

## 报告必须包含

1. **结论**：通过 / 失败（若失败，简要说明原因）。
2. **数量**：总用例数、通过数、失败数、跳过数（如有）。
3. **耗时**：总运行时间（若终端有输出）。
4. **失败详情**（若有）：列出失败用例名称与所在文件，便于定位。
5. **可选**：若使用了 `npm run test:report`，会生成 `test-results/results.json`；可据此解析并生成报告，并在报告中注明该路径。

## 报告格式示例

```markdown
## 测试报告

- **结论**：通过
- **用例**：213 通过，0 失败，0 跳过
- **耗时**：约 46s
- **结果文件**：`test-results/results.json`（如有）
```

若有失败，应增加「失败用例」小节，列出 test suite 与 test name。
