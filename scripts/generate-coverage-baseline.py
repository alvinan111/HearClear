"""
Python 脚本：生成项目改造后的覆盖率报告
用法: python scripts/generate-coverage-baseline.py\n"""\n\nimport json\nimport os\nfrom pathlib import Path\nfrom datetime import datetime\n\ndef analyze_files(root_dir=\"src\", exclude_patterns=None):\n    \"\"\"分析源文件并生成覆盖率基线\"\"\"\n    if exclude_patterns is None:\n        exclude_patterns = [\n            \"__mocks__\",\n            \".test.\",\n            \".spec.\",\n            \"node_modules\",\n        ]\n\n    total_files = 0\n    total_lines = 0\n    file_stats = {}\n\n    for root, dirs, files in os.walk(root_dir):\n        # 过滤目录\n        dirs[:] = [\n            d for d in dirs if not any(pattern in d for pattern in exclude_patterns)\n        ]\n\n        for file in files:\n            if not file.endswith(\".ts\") and not file.endswith(\".tsx\"):\n                continue\n\n            # 检查是否应该排除\n            if any(pattern in file for pattern in exclude_patterns):\n                continue\n\n            file_path = os.path.join(root, file)\n            try:\n                with open(file_path, \"r\", encoding=\"utf-8\") as f:\n                    lines = len(f.readlines())\n                    total_files += 1\n                    total_lines += lines\n                    file_stats[file_path] = lines\n            except Exception as e:\n                print(f\"Warning: Could not read {file_path}: {e}\")\n\n    return {\n        \"total_files\": total_files,\n        \"total_lines\": total_lines,\n        \"file_stats\": file_stats,\n        \"analysis_date\": datetime.now().isoformat(),\n    }\n\ndef generate_report(stats):\n    \"\"\"生成可读的报告\"\"\"\n    report = f\"\"\"\n╔════════════════════════════════════════════════════════╗\n║       HearClear 项目代码覆盖率基线分析报告            ║\n╚════════════════════════════════════════════════════════╝\n\n📊 总体统计\n─────────────────────────────────────────────────────────\n总文件数:        {stats['total_files']}\n总代码行数:      {stats['total_lines']:,}\n分析时间:        {stats['analysis_date']}\n\n📁 文件分布\n─────────────────────────────────────────────────────────\n\"\"\"\n\n    # 按目录统计\n    by_dir = {}\n    for file_path, lines in stats[\"file_stats\"].items():\n        dir_name = os.path.dirname(file_path).split(\"/\")[0]\n        if dir_name not in by_dir:\n            by_dir[dir_name] = {\"files\": 0, \"lines\": 0}\n        by_dir[dir_name][\"files\"] += 1\n        by_dir[dir_name][\"lines\"] += lines\n\n    for dir_name in sorted(by_dir.keys()):\n        info = by_dir[dir_name]\n        report += f\"{dir_name:20s}  {info['files']:3d} 文件  {info['lines']:6,d} 行\\n\"\n\n    # 测试覆盖率建议\n    report += f\"\"\"\n\n🎯 推荐的测试覆盖率目标\n─────────────────────────────────────────────────────────\n当前阶段:         40% (关键路径)\n3 个月目标:       70% (所有服务)\n6 个月目标:       85% (包括界面)\n\n📈 覆盖率改进建议\n─────────────────────────────────────────────────────────\n优先级 1 - 必须 (第 1 周)\n  ✓ src/services/api-client.ts (已完成)\n  ✓ src/utils/validators.ts (已完成)\n  ✓ src/services/secure-storage.ts\n  ✓ src/services/auth-enhanced.ts\n\n优先级 2 - 重要 (第 2-3 周)\n  □ src/stores/ (所有 store)\n  □ src/services/logger.ts\n  □ src/services/error-reporter.ts\n\n优先级 3 - 增强 (第 4-8 周)\n  □ src/components/ (UI 组件)\n  □ src/hooks/ (自定义 Hook)\n  □ src/services/ (所有其他服务)\n\n🚀 运行测试\n─────────────────────────────────────────────────────────\n全部测试:         npm test\n监看模式:         npm run test:watch  \n生成覆盖率:       npm run test:coverage\nJSON 输出:        npm run test:report\n\n💡 最佳实践\n─────────────────────────────────────────────────────────\n1. 为每个新功能编写测试（TDD）\n2. 关键路径优先覆盖\n3. 使用 Istanbul 验证覆盖率\n4. 在 CI/CD 中强制最小覆盖率\n5. 定期审查和更新测试\n\n╔════════════════════════════════════════════════════════╗\n║           配置完成！开始编写单元测试吧 🎉            ║\n╚════════════════════════════════════════════════════════╝\n\"\"\"\n\n    return report\n\nif __name__ == \"__main__\":\n    # 分析项目\n    print(\"分析项目结构...\")\n    stats = analyze_files()\n\n    # 生成报告\n    report = generate_report(stats)\n    print(report)\n\n    # 保存到文件\n    report_file = \"docs/COVERAGE_BASELINE.md\"\n    with open(report_file, \"w\", encoding=\"utf-8\") as f:\n        f.write(report)\n    print(f\"\\n✓ 报告已保存到: {report_file}\")\n\n    # 生成 JSON 格式的详细数据\n    json_file = \"docs/coverage-stats.json\"\n    with open(json_file, \"w\", encoding=\"utf-8\") as f:\n        json.dump(stats, f, indent=2, ensure_ascii=False)\n    print(f\"✓ 详细数据已保存到: {json_file}\")\n"